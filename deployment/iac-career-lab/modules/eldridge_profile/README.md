# Eldridge Profile Module

This reusable learning module demonstrates the standard Terraform module structure, typed and
validated variables, locals, a built-in resource, lifecycle conditions, and documented outputs.
It accepts no secret and contacts no provider API. Its `terraform_data` resource exists only in the
selected Terraform or OpenTofu state.
